# M0/M1 Observer evidence report — 2026-08-22

## Outcome

Status: **synthetic observe-only vertical slice PASS**.

`level_1_first_kill` passed fixture adapter → provenance firewall → `ObservationEnvelope` → read-only decision stub → append-only telemetry → `EncounterRecord` → deterministic replay → Markdown Journal.

Evidence classification: `contract_tested`, `replay_tested`. It is not yet `lab_integrated` with a real addon/client source and not `controlled_live_verified`.

## Results

| Check | Result |
|---|---|
| Python isolated runtime | PASS: CPython 3.14.2, local `.venv` |
| JSON Schema Draft 2020-12 | PASS |
| Automated tests | PASS: 17/17 |
| First-kill observations | 8 |
| Read-only decisions | 8, all `OBSERVE_ONLY` |
| Encounter records | 1, PvE victory, quality `unassessed` |
| Telemetry records | 17 JSONL records |
| Replay deterministic round-trip | PASS |
| Replay SHA-256 | `6aff2bdd3b2830498a3668c6129b5a48fbf67f8fffbfc7c86226fb4ae090af4f` |
| Timestamp reconstruction | PASS; future kill/loot facts absent at `game_time_ms=2100` |
| Server-only information firewall | PASS; rejected before telemetry file creation |
| Unknown capability fallback | PASS; deny-by-default |
| Restricted inspect gate | PASS; evidence required |
| LAB → Champion identity isolation | PASS |
| Duplicate telemetry identity | PASS; append rejected |
| Death/result separation | PASS; `defeat` with quality still `unassessed` |
| Input/movement/combat execution | Absent by design |
| Brain dependency boundary | PASS; domain-only internal imports |
| Input/process libraries | PASS; forbidden imports absent |

The first deterministic test exposed and fixed one issue: `EncounterRecord.replay_ref` initially included the physical output filename, causing identical events to hash differently. It now uses a stable semantic session reference.

## Runtime evidence

Ignored local artifacts:

- `data/telemetry/m1-level-1-first-kill.jsonl`
- `data/runtime/m1-level-1-first-kill-journal.md`

They are evidence outputs, not Champion history and not committed fixtures.

## Task status PA-001–PA-016

- PA-001–PA-015: implemented and covered by contract/unit/replay/integration tests.
- PA-016: parity report produced with Anniversary explicitly `unknown/unverified`; no unsupported parity claim.

## Next safe gate

Design the real TBC 2.4.3 addon/client intake protocol as a read-only adapter. Before connecting it, freeze sample payloads as reviewed fixtures and prove they contain no server-only fields. M2 Journal UI, movement and execution remain out of scope.
