# PA-020 — portable quest observer readiness

## Outcome

`READY_FOR_CONTROLLED_LIVE_PROBE — OBSERVE_ONLY`

## Implemented

- SavedVariables schema `0.2`, backward-compatible with the captured `0.1` export;
- addon `PerfectAssassinObserver 0.2.0`;
- client-observed quest NPC dialog and available/active titles;
- quest narrative text, objective text and suggested group;
- quest-log entries, completion state and objective progress;
- self spellbook snapshot with localized name/rank and passive flag;
- strict event-specific schema allowlists;
- normalization of the legacy empty-array `{}` representation only for known list fields;
- negative test rejecting an injected `server_quest_id`;
- no protected action, quest acceptance, chat or external transport API in the addon.

## Fixture evidence

- 5 observations and 5 read-only decisions;
- 0 encounters, because no combat was part of this fixture;
- deterministic replay hash: `8908eca609b31eda7978eddd79abf3c2a2ae78ee5d70dad6efb70555d865e7ac`;
- full suite: 30/30 tests passed.

## Deployment

- client was stopped;
- the previous PA-019 addon matched its pinned hashes;
- the previous addon was backed up under ignored `data/runtime/addon-backups`;
- the PA-019 raw capture was copied under ignored `data/runtime/captures` before the next client session can replace SavedVariables;
- installed addon 0.2 hashes match the repository artifacts;
- installer is idempotent and only permits an explicitly requested upgrade from the known previous artifact.

## Evidence boundary

Status is `contract_tested + replay_tested + deployed_to_client`. Quest and spellbook capabilities are not `controlled_live_verified` until the operator completes the probe card in `docs/15-pa020-portable-quest-observer.md` and the resulting Journal is reconciled with the screen.

No Zygor package or AI API key is required for this probe. Those are separate providers behind the accepted quest-intelligence boundary.
