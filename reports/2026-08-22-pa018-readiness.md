# PA-018 — controlled client probe readiness

## Outcome

`READY_FOR_OPERATOR_PROBE — not controlled_live_verified`

## Passed

- client is statically `2.4.3.8606` and `realmlist.wtf` is `127.0.0.1`;
- no `Wow.exe` process existed during deployment;
- addon installed to the exact `Interface\AddOns\PerfectAssassinObserver` target;
- installed TOC and Lua hashes equal PA-017 artifacts;
- installer is idempotent and refuses mismatched overwrite;
- bounded rollback targets only the observer addon directory and supports `-WhatIf`;
- CMaNGOS database, realm, world and RA listeners are all loopback-only and ready;
- server reports CMaNGOS-TBC/2.4.3 world database and zero online players;
- `PA_OBSERVER` exists with expansion 1 and GM level 0;
- observer password generation is capped at 14 characters for the legacy 16-character client limit;
- observer account secret is outside Git with ACL limited to the operator and SYSTEM.
- full local suite: 27/27 tests passed, including installer-to-artifact hash pinning.

## Safety hold

Codex did not execute `Wow.exe`. Authenticode status is `HashMismatch`, so the real client probe is an explicit manual operator decision. Until normal logout produces a real SavedVariables file and the resulting Journal is reconciled with the screen, evidence remains `deployed_to_client + lab_ready`, not `controlled_live_verified`.

## Service state at handoff

LAB services were intentionally left running for the bounded probe:

- MariaDB: `127.0.0.1:3307`;
- realmd: `127.0.0.1:3724`;
- mangosd world: `127.0.0.1:8085`;
- remote administration: `127.0.0.1:3443`.

After the probe, use the graceful stop order documented in `docs/11-tbc-lab-bootstrap.md`.

## Post-readiness update

The operator completed the bounded probe successfully. The resulting PA-019 evidence and current capability boundary are recorded in `reports/2026-08-22-pa019-first-live-observer-capture.md`; this report remains the historical pre-probe readiness record.
